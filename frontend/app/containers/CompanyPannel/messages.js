/*
 * CompanyPannel Messages
 *
 * This contains all the text for the CompanyPannel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CompanyPannel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CompanyPannel container!',
  },
});

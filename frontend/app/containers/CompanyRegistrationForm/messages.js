/*
 * CompanyRegistrationForm Messages
 *
 * This contains all the text for the CompanyRegistrationForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CompanyRegistrationForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CompanyRegistrationForm container!',
  },
});

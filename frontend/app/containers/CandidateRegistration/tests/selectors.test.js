// import { selectCandidateRegistrationDomain } from '../selectors';

describe('selectCandidateRegistrationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
